import { CodpostUnicoDirective } from './codpost-unico.directive';

describe('CodpostUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new CodpostUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
