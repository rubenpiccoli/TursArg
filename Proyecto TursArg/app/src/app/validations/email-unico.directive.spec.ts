import { EmailUnicoDirective } from './email-unico.directive';

describe('EmailUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
