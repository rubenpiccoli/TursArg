import { FiltroCiudadPipe } from './filtro-ciudad.pipe';

describe('FiltroCiudadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCiudadPipe();
    expect(pipe).toBeTruthy();
  });
});
